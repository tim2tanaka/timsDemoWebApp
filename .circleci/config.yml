version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/node:7.10
    working_directory: ~/repo
    steps:
      - checkout
      - run: yarn install
  test:
    docker:
        # specify the version you desire here
        - image: circleci/node:7.10
      working_directory: ~/repo
    steps:
      # run tests!
      - run: yarn test

workflows:
  version: 2

  pipeline_dev:
    jobs:
      - build
      - test:
          requires:
            - build
